export GAZEBO_MODEL_PATH="$(catkin_find --first-only khm_gazebo models):${GAZEBO_MODEL_PATH}"
export GAZEBO_RESOURCE_PATH="/usr/share/gazebo-9:$(catkin_find --first-only khm_gazebo):${GAZEBO_RESOURCE_PATH}"